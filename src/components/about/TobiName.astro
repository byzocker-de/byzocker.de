<span class="nameSpan">
    <span id="nameTobi" class="nameTobi">tobi</span>
    <span class="nameWaving">👋</span>
</span>

<style>
    .nameTobi {
        font-weight: 700;
        background: linear-gradient(
            45deg,
            var(--white),
            var(--blue),
            var(--pink),
            var(--white)
        );
        background-size: 200%;
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        animation: background-pan 2s cubic-bezier(.12,.76,.92,.63) infinite; 
    }
    .nameWaving {
        display: inline-block;
        animation: waving 1s linear infinite;
    }
    @keyframes waving {
        0% {
            transform: rotate(0deg);
        }
        50% {
            transform: rotate(-20deg);
        }
        100% {
            transform: rotate(0deg);
        }
    }
    @keyframes background-pan {
        from {
            background-position: 0% center;
        }

        to {
            background-position: -200% center;
        }
    }
</style>
